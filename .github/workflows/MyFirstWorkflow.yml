name: My First Workflow 2
# This workflow says Hello World and then lists all files in the repo
on: 
  #push:
  #   branches: 
  #     - main
  workflow_dispatch:
jobs:
  MyFirstJob:
    runs-on: ubuntu-latest

    steps:
      - run: echo "👋 Hello GHA World!"

      - name: Checkout
        uses: actions/checkout@v6.0.2

      - name: List files in repository
        run: |
          echo "Repository ${{ github.repository }} contains the following files:"
          tree
